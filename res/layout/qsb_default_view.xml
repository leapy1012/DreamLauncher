<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/coloros_hotseat_search_background"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="14dp"
        android:paddingEnd="14dp">

        <ImageView
            android:id="@+id/btn_qsb_search"
            android:layout_width="40dp"
            android:layout_height="match_parent"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:contentDescription="@string/abandoned_search"
            android:padding="8dp"
            android:src="@drawable/coloros_ic_hotseat_search" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:paddingStart="4dp"
            android:text="@string/abandoned_search"
            android:textColor="#CCFFFFFF"
            android:textSize="14sp" />

        <ImageView
            android:id="@+id/btn_qsb_setup"
            android:layout_width="40dp"
            android:layout_height="match_parent"
            android:contentDescription="@string/gadget_setup_text"
            android:padding="8dp"
            android:src="@drawable/ic_setting"
            android:tint="#CCFFFFFF"
            android:visibility="gone" />
    </LinearLayout>
</FrameLayout>
